package main_test

import "testing"

func TestStudentList(t *testing.T) {
	t.Run("when there are students", func(t *testing.T) {
		//student1 := student.New("John Doe")
		//student2 := student.New("Jane Doe")
		t.Run("it shows the list of students", func(t *testing.T) {
			// Add students
			// Render page
			// Verify the student names are in the page
			t.Fail()
		})
	})
}